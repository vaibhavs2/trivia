/**
 * @format
 */

import {AppRegistry} from 'react-native';
import QuestionScreen from './src/screens/questionScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => QuestionScreen);
